services:
  - type: web
    name: markening-agent
    runtime: node
    plan: free
    # Chromium нужен для авто-обложек в Telegram; если не установится — посты уйдут текстом
    buildCommand: npm ci && (npx playwright-core install chromium || true) && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: "22"
      - key: AUTH_SECRET
        generateValue: true
      - key: ADMIN_LOGIN
        sync: false
      - key: ADMIN_PASSWORD
        sync: false
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: TELEGRAM_CHANNEL_ID
        sync: false
      - key: ANTHROPIC_API_KEY
        sync: false
